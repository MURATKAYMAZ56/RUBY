=begin
- procs?  
   is short for procedure.
   an object that essentialy serves as a seved blocks
   insated of using block again and again use procs

- procs vs blocks
  - block is a part of method call , can only pass one block in a method,
    no way to call the block
  -procs is an OBJECT  an inastance of the proc class , can pass multiple procss to a method as arg
    use the call method

- lambdas 
    -same as proc  but differs in how it treats its arguments and return the keyword
    -
- procs vs lambdas
  procs: ignore any returns arguments, return outside the methid
  lambdas: validates the number of arguments, return control back to the method

=end
